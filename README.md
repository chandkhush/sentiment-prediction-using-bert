# sentiment-prediction-using-bert
BERT-based sentiment analysis model built using PyTorch and Transformers. This project preprocesses text, fine-tunes BERT on a sentiment dataset, and predicts polarity (positive/negative). Includes training scripts, evaluation, and inference notebook.
